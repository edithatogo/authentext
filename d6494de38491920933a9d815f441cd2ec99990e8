Task: Add non-publishing guidance drift receipt and review workflow. Changes: added deterministic metadata-only drift receipts and complete review-decision validation while keeping publication and guidance activation explicitly disabled. Why: monitoring may flag upstream changes but cannot publish or silently alter active guidance. Verification: drift/source tests; npm run lint:all; npm test (140 tests plus integrations via commit hook); npm run test:coverage (86.19% lines, 91.18% functions, 76.18% branches).
